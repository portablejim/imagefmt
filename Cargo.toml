[package]
name = "imagefmt"
version = "0.1.0"
authors = ["Tero Hänninen"]
description = "Image loading and saving, 8-bit only, with conversions."
license = "MIT"
repository = "https://github.com/lgvz/imagefmt"
include = ["imagefmt.rs", "Cargo.toml", "README.md"]

[lib]
name = "imagefmt"
path = "imagefmt.rs"

[dependencies]
flate2 = "0.2"
